name: Github Workflow

on:
  push:
    branches:
      - main

env:
  NAME: "MUHAMMED JIMOH"
  PROFESSION: "DATA ENGINEER"
  PASSION: "DATA ENTHUSIAST"

jobs:
  builds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Echo Github Actions
        run: echo "Hi, this is $NAME I am a $PROFESSION by profession and $PASSION is my passion."

  post-builds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Echo Post-build Steps
        run: echo "POST-BUILD STEPS AFTER EXCUTION OF THE ABOVE STEPS"
